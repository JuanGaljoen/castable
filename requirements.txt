# Geometry validation + test toolchain (RNG-1)
trimesh==4.5.3
numpy==2.2.1
networkx==3.4.2
scipy==1.15.0
pytest==8.3.4

# Backend (RNG-2)
flask==3.1.3

# Geometry kernel (RNG-15) — build123d is the shipping kernel; every module under
# ringcad/geometry/ imports it. cadquery-ocp-novtk is the OpenCASCADE binding
# build123d is built on: transitive, but pinned deliberately. build123d only
# requires >=7.9,<8.0, and an unpinned OpenCASCADE means a clean install months
# apart can produce different meshes — which the parity and watertightness tests
# would then fail in a confusing way. Bump the two together. (RNG-18)
build123d==0.11.0
cadquery-ocp-novtk==7.9.3.1.1

# RingSpec schema + vision structured output (RNG-14, RNG-6)
pydantic==2.13.4

# Photo classification (RNG-6)
anthropic==0.109.1

# Load ANTHROPIC_API_KEY from a local .env at startup (RNG-21)
python-dotenv==1.0.1
